/* eslint-disable */
const config = {
  x: 0,
  y: 0,
  vx: 0,
  vy: 0
}
export class Spirit {
  constructor(params = config) {
    this.x = params.x;
    this.y = params.y;
    this.vx = params.vx;
    this.vy = params.vy;
  }
  render() {
    
  }
}
